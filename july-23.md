### Finding [added from Jul 22]: why are we showing a stale customer record on the frontend?
- We can see in the earlier payload that we were getting the customer role directly after the view entity has arranged that for us via a service, but now, when we use a direct and automatic entity facade response, we see that we get everything and need to apply filtering from our end that is frontend.
- We can get data from the backend in several ways, like through data documents, service(view entity), REST entity //[check perform find usage - reference]. When to use which one of them, what is the ideal case and best practice? 
-THIS WAS DISCUSSED IN SCRUM
- Check Dynamic Data doc.- test dataset for a record
- To document this

- to check SM - SQL purging findings
- virtual product and variants association, scenario: if a  virtual product got skipped (there was dependency with the earlier virtual/var)
